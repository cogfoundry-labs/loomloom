# Inputs

`templateInputs` defines values supplied by a caller. The key is stable machine identity; `presentation` controls Workbook and UI display.

- value: string, number, integer, boolean, array, or object.
- artifact: a platform-managed image, audio, video, document, or collection.

Template constraints form the public domain. Core requires it to be a safe subset of every bound contract port.
