{
  "meta": {"name": "Single-Step Text Generation", "description": "Generate a short article from a topic pasted by the user"},
  "steps": [
    {
      "stepId": "stp_write01",
      "displayName": "Generate Article",
      "executionUnit": "text-generate",
      "instruction": "Generate a concise article based on the user's topic.",
      "defaultModelRef": {"modelKey": "google/gemini-2.5-flash"}
    }
  ],
  "inputSchema": {
    "fields": [
      {"key": "topic", "label": "Topic", "valueType": "string", "required": true, "order": 1}
    ]
  },
  "fieldBindings": [
    {"fieldKey": "topic", "stepId": "stp_write01", "paramKey": "prompt", "bindMode": "shared"}
  ]
}
