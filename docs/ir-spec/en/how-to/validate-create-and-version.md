# Validate, create, and version

Run `loomloom template-spec check template.json`, then create with `specVersion=template-spec/v2` and `canonicalSpecV2`. Core resolves and freezes current authority contracts.

Append a new immutable TemplateVersion for changes. Download a new Workbook and validate/precheck before submitting a Run.
