# Design Workbook inputs

- Name inputs by business meaning, not by a bare model parameter name.
- Define kind, type, required/blank policy, and presentation order.
- Explicitly bind one input to multiple Step ports when sharing is intentional.
- Give Artifact inputs exact MIME and cardinality.
- A Profile model input must allow blank so the default can apply.
