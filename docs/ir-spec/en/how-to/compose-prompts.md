# Compose prompts

Use `composeValue` when an author fixes instructions and a caller supplies variables. v2 supports deterministic string `concat` with literal and string Template Input parts. It is not a general expression language.
