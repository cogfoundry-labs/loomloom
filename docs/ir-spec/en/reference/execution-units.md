# Execution contracts and ports

TemplateSpec v2 does not expose a static public Execution Unit list. Authors reference a fixed-model Subject or Capability Profile. Core resolves stable input/output port IDs, scalar constraints, Artifact MIME/cardinality, native mappings, adapter, and execution surface version.

Query the target environment for authority records. Save-time resolution freezes them and rejects catalog or execution-surface drift.
