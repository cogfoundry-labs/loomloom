# 输入

`templateInputs` 定义运行者能提供的值。key 是稳定机器身份，`presentation` 是 Workbook 和 UI 展示信息。

- value：字符串、数字、整数、布尔、数组或对象。
- artifact：平台管理的图片、音频、视频、文档等资产集合。

输入约束属于模板公开域。绑定到模型端口时，Core 要求模板值域是目标合同值域的安全子集；作者可以收窄，不能放宽。
