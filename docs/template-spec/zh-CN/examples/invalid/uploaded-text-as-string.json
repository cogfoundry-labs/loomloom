{
  "meta": {"name": "错误的上传文本字段"},
  "steps": [
    {
      "stepId": "stp_extract1",
      "displayName": "提取事实",
      "executionUnit": "text-generate",
      "defaultModelRef": {"modelKey": "google/gemini-2.5-flash"}
    }
  ],
  "inputSchema": {
    "fields": [
      {
        "key": "source_text",
        "label": "原始材料",
        "description": "上传文本文件后填写返回的 ia_xxx 资产 ID",
        "valueType": "string",
        "required": true
      }
    ]
  },
  "fieldBindings": [
    {"fieldKey": "source_text", "stepId": "stp_extract1", "paramKey": "prompt", "bindMode": "shared"}
  ]
}
