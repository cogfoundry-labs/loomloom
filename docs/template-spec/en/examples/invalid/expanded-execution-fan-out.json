{
  "meta": {"name": "Historical multi-value expansion", "description": "This syntax is only for historical runtime compatibility"},
  "steps": [
    {
      "stepId": "stp_write01",
      "displayName": "Generate each article",
      "executionUnit": "text-generate",
      "instruction": "Generate one article for each topic.",
      "defaultModelRef": {"modelKey": "google/gemini-2.5-flash"}
    }
  ],
  "inputSchema": {
    "fields": [
      {"key": "topics", "label": "Topics", "valueType": "string", "required": true, "multiValue": true, "maxValues": 10, "order": 1}
    ]
  },
  "fieldBindings": [
    {"fieldKey": "topics", "stepId": "stp_write01", "paramKey": "prompt", "bindMode": "expanded"}
  ]
}
