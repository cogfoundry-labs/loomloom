{
  "meta": {"name": "Invalid Uploaded Text Field"},
  "steps": [
    {
      "stepId": "stp_extract1",
      "displayName": "Extract Facts",
      "executionUnit": "text-generate",
      "defaultModelRef": {"modelKey": "google/gemini-2.5-flash"}
    }
  ],
  "inputSchema": {
    "fields": [
      {
        "key": "source_text",
        "label": "Source Material",
        "description": "Upload a text file and enter the returned ia_xxx asset ID",
        "valueType": "string",
        "required": true
      }
    ]
  },
  "fieldBindings": [
    {"fieldKey": "source_text", "stepId": "stp_extract1", "paramKey": "prompt", "bindMode": "shared"}
  ]
}
