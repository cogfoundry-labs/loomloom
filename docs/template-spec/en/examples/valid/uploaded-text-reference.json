{
  "meta": {"name": "Uploaded Text Analysis", "description": "Read an uploaded text file and extract facts"},
  "steps": [
    {
      "stepId": "stp_extract1",
      "displayName": "Extract Facts",
      "executionUnit": "text-generate",
      "instruction": "Extract only facts explicitly present in the referenced material. Do not invent missing information.",
      "defaultModelRef": {"modelKey": "google/gemini-2.5-flash"},
      "upstreamBindings": [
        {"inputPort": "reference", "sourceType": "initial_input", "sourceInputKey": "source_text"}
      ]
    }
  ],
  "inputSchema": {
    "fields": [
      {
        "key": "source_text",
        "label": "Source Material",
        "description": "Upload a UTF-8 text file and provide the returned asset ID",
        "valueType": "text_reference",
        "acceptedMimeTypes": ["text/plain"],
        "required": true,
        "order": 1
      }
    ]
  }
}
